# 更新日志

## 2026-07-25 v0.17.0 上下文智能与隐私控制

### 版本概述

CraftAgent v0.17.0 汇总自 v0.16.0 以来的上下文能力、隐私控制与工作连续性升级。面向用户提供更易理解的隐私控制中心、更清晰的上下文使用透明度，以及更顺畅的项目与会话恢复体验。

内部迭代阶段不再单独对外编号。

### 新增

- 上下文智能能力升级：更好地连接会话、浏览器与项目状态，辅助理解当前工作与后续方向
- 隐私与上下文控制中心：区分「自动形成上下文」与「主动使用内容」
- 上下文访问透明度：可查看最近访问的来源、用途与结果（仅元数据）
- 工作恢复体验优化：项目续做、最近项目入口、项目相关网页等连续性能力

### 优化

- 重构隐私设置页面，简化权限表达，隐藏未接入的开发状态选项
- 优化 Explore / Library 工作流程与项目作用域体验
- 优化会话与项目上下文的连接与建议呈现
- 统一部分界面组件与文案表现，提升整体稳定性

### 安全

- 敏感信息保护机制保持有效（密码、API 密钥、Cookie 等不会写入上下文系统）
- 权限控制体验优化（后台自动采集与主动操作分开展示）
- 上下文访问审计（决策元数据记录，不含正文、Diff 或密钥）
- 资源库从会话生成等场景继续使用用户确认机制

### 修复

- 修复偏好设置保存时可能覆盖并丢失部分配置的问题
- 修复资源库列表显示内部项目标识、改为显示项目名称
- 修复项目活动中相关网页链接的可点击状态
- 修复部分设置页与聊天界面硬编码英文文案
- 修复看板任务编辑器按钮样式不一致
- 统一资源库删除确认等对话框交互
- 修复若干界面与交互细节问题

### Breaking Changes

无。

---

## 2026-07-24 v0.16.0 隐私控制、Today 收敛与资源库

### 版本概述

v0.16.0 在 Cognition Layer 之上补齐三项用户可感知能力：

1. **隐私与上下文控制** — 决定系统能感知什么、何时感知
2. **Today 收敛** — 去掉重复 Brief / 独立建议流，聚焦待处理与最近使用
3. **资源库** — 把会话整理成可编辑、可版本、可导出的文档

本版本不包含：会话增量同步、Sync Proposal / Diff、GLM 视觉、Computer Use、多人协作与云端公开分享。

---

### 新增功能

#### 隐私与上下文控制

- 上下文感知总开关、Today 使用上下文开关
- 临时隐私模式
- 按来源的数据权限与 Privacy Filter
- 上下文访问日志（不含正文）
- Cognition 派生数据清理（白名单，不误删会话 / 资源库 / MEMORY.md / 浏览器 Profile）

#### Today 收敛

- 可选会话输入框、可选 Today 区域
- 待处理队列、最近使用、Snooze、完成并归档
- 移除首页 Explore Brief / 独立 Guidance 与重复推荐流

#### 资源库

- 左侧资源库入口
- 空白文档；从会话 **AI 整理** 或 **原样保存**
- 结构化内容保真（表格、代码、引用等）
- 来源关联与章节锚点（用户界面不可见）
- 编辑 / 预览、基础版本、归档删除
- Markdown / HTML / PDF 导出
- Consent Token 授权与生成透明信息

---

### 已知限制

- 超长会话生成文档可能要求选择消息范围
- 来源跳转暂时只打开会话，未必精确滚动到消息
- 极端复杂 Markdown 的 HTML/PDF 导出可能存在样式差异

---

## 2026-07-23 v0.15.0 Cognition Layer 认知层

### 版本概述

v0.15.0 引入全新的 Cognition Layer（认知层），为 Craft Agents 建立长期工作状态理解能力。

本版本让 CA 从单纯的 AI 对话与执行工具，进一步演进为个人 AI 工作中枢。

核心认知链：

```
Event
 ↓
Observation
 ↓
Loop
 ↓
Reflection
 ↓
Guidance
 ↓
Today
```

通过这套链路，CA 可以记录工作过程、理解当前状态、发现未完成事项，并提供可解释的下一步建议。


---

### 新增功能


#### 🧠 Cognition Event Ledger 事件账本

新增统一工作事件记录系统。

支持记录：

- 会话生命周期
- Checkpoint
- Git 工作状态
- 浏览器工作上下文


特点：

- 统一事件入口
- 追加式存储
- 保留事件来源
- 支持诊断与恢复


---

#### 🔎 Observation 工作观察层

新增 Observation 层。

将原始事件转换为稳定的工作事实。


例如：

Git 提交代码：

↓

项目正在推进


浏览器查看资料：

↓

正在研究相关技术方向


Session checkpoint：

↓

识别当前进度和阻塞。


所有 Observation 均保留来源信息，确保结果可追溯。


---

#### 🔁 Loop 工作闭环系统

新增 Loop 管理系统。

Loop 表示：

> 仍然需要用户关注、尚未完成闭环的工作事项。


支持状态：

- Open
- Waiting
- Blocked
- Stale
- Resolved
- Dismissed


新增：

- 自动发现
- 去重处理
- 状态保护
- 生命周期管理


已解决或已关闭事项不会被重复生成。


---

#### 📝 Reflection 工作复盘

新增 Reflection 能力。


支持：

##### 任务复盘

生成：

- 已完成内容
- 当前进展
- 未解决问题
- 阻塞原因
- 下一步方向


##### 每日复盘

生成：

- 最近完成事项
- 当前关注重点
- 后续推进方向


所有 Reflection 均保留来源引用。


---

#### 💡 Guidance 主动建议系统

新增 Guidance。

CA 可以根据：

- 当前 Loop
- 工作进展
- 最近活动
- 阻塞信息


生成下一步建议。


每条建议包含：

- 建议内容
- 推荐原因
- 关联上下文
- 来源依据


Guidance 只提供建议：

不会自动创建任务。

不会自动修改用户状态。


---

#### 📅 Today 工作入口升级

Today 接入 Cognition Guidance。


现在 Today 同时展示：

- 用户任务
- AI 工作建议


支持：

- 查看推荐原因
- 查看关联上下文
- 快速恢复工作状态


保持任务系统与认知系统分离。


---

#### 🔧 Git 工作感知

新增 Git Event Provider。


支持：

- Commit
- Push
- Sync
- Branch
- PR
- Dirty Changes


Git 活动现在可以进入完整认知链：


```
Git
 ↓
Event
 ↓
Observation
 ↓
Loop
 ↓
Guidance
```


让 CA 能理解开发工作的真实推进情况。


---

#### 🌐 浏览器工作上下文感知

新增 Browser Context Provider。


支持：

- 页面打开
- 标签关联 Session
- 收藏资料
- 页面关闭


用于理解：

- 用户正在研究什么
- 当前工作背景是什么


隐私保护：

不会保存：

- 密码
- Cookie
- Token
- 页面完整内容


---

#### 🔍 Cognition Debug

新增认知调试能力。


支持查看完整链路：

```
Event
 ↓
Observation
 ↓
Loop
 ↓
Reflection
 ↓
Guidance
```


用于：

- 调试推荐原因
- 查看数据来源
- 分析认知状态


---

### 工程改进

- 新增认知数据迁移
- 完善 Cognition IPC 接口
- 增强诊断能力
- 增加认知链测试覆盖
- 优化多平台构建流程
- 增加数据修复与维护能力


---

### 测试

已验证：

- Shared Cognition 测试
- Server Core Cognition 测试
- Today / Guidance 集成测试
- 类型检查


---

### 版本意义

v0.15.0 是 Craft Agents 的重要架构升级。

从：

> 一个能够调用 AI 的桌面 Agent


升级为：

> 一个能够理解个人工作状态的 AI 工作中枢。


CA 现在可以持续理解：

- 你正在做什么
- 你已经完成什么
- 哪些事情仍未闭环
- 下一步最值得关注什么

## 2026-07-23 v0.14.0 消息平台精简、体验修复与工程收敛

本版本将消息接入收敛到实际在用的飞书与微信，清理历史残留，并继续修补输入体验与文档。

### 破坏性变更

- 完整移除 WhatsApp 与 Telegram（UI、Adapter、Worker、IPC、依赖与专属文案）。现役消息平台为飞书 / Lark 与微信。
- 旧工作区中的 WhatsApp / Telegram 配置与绑定会被识别为 unsupported：不初始化、不崩溃、不阻塞其他平台，并记录一次明确提示。
- 安装包不再包含 messaging-whatsapp-worker / Baileys，也不再依赖 grammy。

### 消息平台

- 设置页、会话菜单、配对与访问控制入口仅保留飞书与微信。
- 保留通用 messaging gateway（Adapter / Registry / Binding / Config Store），便于后续按注册方式扩展新平台。
- 自动化中的 Telegram forum topic 能力下线；飞书与微信绑定与收发路径保持现役。

### 体验与文档

- 修复切到其他 app 再切回后，中文输入首字符脱离 IME 组合的问题。
- 设置 → 标签：创建改为标题下方内联编辑（名称 + 系统色），去掉弹窗；「标签层级」与「自动应用规则」统一「添加」按钮；保留行内添加子标签与现有层级 / 自动规则能力。
- README 能力表与特色章节补全；消息接入说明同步为飞书与微信。

### 工程清理

- 删除孤儿 UI 组件、重复 onboarding 实现、未装配 TipTap 扩展、无用依赖，以及 design-qa 审计资产。
- 清理已证明无调用链的死协议表面（早期 WhatsApp 子进程 RPC 常量、`LLM_Connection:refreshModels` RPC 壳、`onboarding:validateMcp` RPC 壳）；保留 SAVE 路径内 `refreshNow()` 与 `validateMcpConnection` 库函数。
- 加强测试隔离与启动/退出路径稳定性；版本统一 `0.14.0`；IPC 穷尽、i18n、typecheck、正式构建验证通过。

## 2026-07-22 v0.13.0 长期任务工作台、Mermaid 与浏览器增强

- 新增长期任务状态：会话可设置目标、优先级、截止时间和提醒；Agent 每轮自动留下检查点（完成情况、下一步、阻塞项、相关文件），支持手保存检查点并根据最近停点继续执行。
- 探索首页新增「今天」任务队列：集中呈现到期提醒、逾期事项、失败/等待中的执行、未读进展和可继续任务，结合优先级与活动智能排序。
- 任务提醒通过系统通知触达，设置支持提醒开关、建议开关和免打扰时段。
- Markdown 图表引擎升级到 Mermaid 官方完整版，支持思维导图、时间线、甘特图、Sankey、Architecture 等全部官方语法。
- 浏览器标签右键菜单新增收藏/置顶/静音/批量关闭操作，置顶状态随工作区保存并稳定排列在标签列表顶部。
- 会话搜索扩展到任务目标、项目目录、标签、检查点摘要和阻塞项。
- 看板卡片补充目标、检查点、优先级与截止状态；自动化会话和远程迁移同步完整任务上下文。
- 探索模式实时恢复：重启后回到离开前的工作模式；折叠侧栏仍保留模式切换入口。
- 修复任务元数据被旧缓存覆盖、历史提醒漏出、提醒重复投递、标签右键白屏、标签关闭回退不稳定等问题。
- 补齐德/英/西/匈/日/波兰语长期任务、提醒和浏览器标签文案。

## 2026-07-22 v0.12.0 任务工作台、Git 环境与长期上下文

- 打通浏览器与会话的双向工作台：浏览器标签右键菜单可直接打开或关闭右侧会话，审阅侧栏统一承载会话、浏览器、文件、终端、来源、快捷键、画布与插件组件；网页主视图移除常驻标题栏并恢复全高显示，单纯浏览仍保持独立。
- 重组任务信息与会话状态：右上角信息面板集中呈现上下文、任务上下文、会话文件、输出、来源和项目 Git 环境；输入框旁「会话状态」统一承载标题、会话文件、最近停点和模型用量。
- 新增完整 Git 工作流：变更与差异、暂存/取消暂存、提交、切换与新建分支、拉取、推送、同步、分支比较、GitHub PR 状态与创建；结合项目文件、代码工具和开发意图智能显示与排序，普通对话和非 Git 目录自动隐藏。
- 浏览器工作区快照持久化任务归属，重启后恢复网页时会重新绑定原任务，保留浏览器作为 Agent 工具的上下文连续性。
- 项目页新增长期记忆编辑入口，直接管理自动注入项目任务的 `MEMORY.md`，让记忆内容与调用范围可见、可控。
- 消息网关、自动化等非桌面入口的附件由会话层统一落盘，重启和跨客户端恢复后仍能显示并继续使用。
- 收敛探索导航菜单：网页筛选移除重复的新建入口，会话筛选将「看板」调整为搜索下方的单一入口，修正「会话 / 网页」切换菜单的边界与左侧对齐，并在主侧栏折叠后继续保留切换器。
- 统一信息面板的分区标题、卡片间距、中文 Git 操作和自动隐藏细滚动条，减少重复内容与视觉干扰。
- 版本统一升级到 `0.12.0`，同步所有 workspace 清单、锁文件和 README 版本标记。

## 2026-07-21 v0.11.10 探索工作台、附件上下文与设置收口

### 探索与网页工作区

- 将探索统一为「会话 / 网页」双模式入口，中间列表随模式切换；探索首页不再错误高亮历史会话或网页标签。
- 主导航创建按钮跟随当前模式显示「新建会话」或「新建网页」，移除重复的「所有会话」一级入口。
- 探索首页重做为三段式工作台：统一输入、默认模型生成的近期工作概览，以及 3–5 条可继续执行的建议；分析频率、开关和建议数量可在设置中管理。
- 浏览器空白页简化为固定地址/搜索输入框：网址直接访问，普通文本使用 Google 搜索；空白项显示「新标签页」且不重复副标题，加载后显示完整当前地址。
- 应用退出前保存实时网页标签、活动标签、图标、访问时间与页面状态，并刷新 Chromium 存储；退出销毁不再把恢复快照覆盖为空，重启后继续保留网页会话与登录态。

### 会话附件与快捷操作

- 会话输入支持选择文件夹作为上下文，也支持从 Finder 直接拖入文件夹；文件夹与普通文件共用附件预览、去重、删除和草稿恢复流程。
- 附件预览按文件夹、图片、视频、音频、PDF、文档及其他文件类型展示对应图标和元信息，文件夹以引用语法传入会话而不是作为二进制上传。
- 会话右上角新增快捷键入口，复用右侧审阅面板展示原有快捷键页面，重复打开时聚焦已有面板。

### 设置与标签管理

- 设置导航改为单层分组列表，移除重复的父子选中状态，并保留全部原设置页面和独立窗口能力。
- 标签从主导航移入设置，支持新增、删除、层级管理以及查看和维护标签所包含的会话；会话筛选中的标签入口继续保留。
- 探索设置加入工作概览开关、刷新周期和建议数量；信息页、插件页和数据表同步收敛到现有设计 token。

### macOS 安装与工程整理

- macOS DMG 改用 Retina 双分辨率安装背景，窗口、应用图标、Applications 链接和拖拽提示按参考安装器尺寸对齐。
- 版本统一升级到 `0.11.10`，同步所有 workspace 清单、锁文件和 README 版本标记，修复上个版本遗留的元数据不一致。
- 补充探索分析、文件夹附件、浏览器退出持久化、设置导航和关键视觉状态的测试与设计验收记录。

### 验证

- Renderer 正式构建、i18n parity/sort/coverage、版本一致性和差异检查通过。
- 浏览器输入、探索分析、文件夹附件、退出持久化与自动更新策略专项测试通过。

## 2026-07-19 v0.11.9 探索智能入口与升级体验优化

### 探索智能入口

- 浏览器新标签页升级为「探索」默认首页，自动识别输入意图：网址 → 导航、短文本 → 搜索、自然语言 → 智能问答。
- 意图分类使用隐私安全的特征签名，支持用户手动纠正；纠正模式学习 2 次以上且一致后自动生效。
- 智能问答复用完整会话界面，网页历史与最近会话统一展示。
- 修复无附件问答的 `attachments is not iterable` 兼容问题和入口复位逻辑。

### 升级体验

- 诊断包新增更新渠道、平台清单、下载状态、缓存迁移和防降级状态。
- 升级后自动展示最新动态（仅加载正式版本文件），读过后不再重复打扰。

### 工程优化

- 浮层、通知与 WebContentsView 暂停协调提取为独立 `useNativeViewSuspension` Hook。

## 2026-07-19 v0.11.8 更新缓存防回退修复

### 根因

- `electron-updater` 在设置 `channel` 时会隐式将 `allowDowngrade` 改为 `true`，而 0.11.7 没有在赋值后重新关闭。
- 0.11.7 将 updater 缓存中任意安装包视为已下载更新，未确认缓存版本严格高于当前版本。
- `autoInstallOnAppQuit` 允许普通退出时安装旧 pending 包；自定义缓存探测路径也没有使用打包生成的 `updaterCacheDirName`。

### 修复

- 设置 `channel` 后显式恢复 `allowDowngrade = false`，并关闭普通退出自动安装；用户确认安装后仍由 `quitAndInstall` 完成自动替换与重启。
- 下载完成和安装前均验证版本必须严格高于当前版本，并且与本次 YAML 清单版本完全一致，否则拒绝并清理 pending 缓存。
- 0.11.8 首次启动从打包的 `app-update.yml` 解析真实缓存目录，自动清理旧 pending 包并写入一次性迁移标记。
- 移除“只要缓存目录里存在安装包就判定 ready”的不安全回退，改为只信任 electron-updater 校验完成后的事件。

### 迁移

- 已被回退到 0.11.6 或仍在 0.11.7 的用户，只需手动安装一次 0.11.8，无需手动删除缓存。
- 从 0.11.8 升级到后续版本时，再验证 Windows/Linux 的正常自动下载、确认安装与重启链路。

## 2026-07-19 v0.11.7 本地更新渠道与维护基础设施

### 本地版更新

- `electron-builder` 保留 generic provider，但 feed 改为个人仓库 GitHub Latest Release 的 `releases/latest/download`；Windows、macOS、Linux 分别读取 `latest.yml`、`latest-mac.yml`、`latest-linux.yml`，版本只比较 YAML 的 `version`，不解析 `vX.Y.Z-local` tag。
- Windows x64 保留后台下载、退出安装和重启；Linux x64 在自动替换前验证 `APPIMAGE` 路径，环境不成立或更新失败时提供 GitHub Release 手动下载恢复操作。
- 未签名 macOS 构建只检查新版本并展示 GitHub Release 手动下载入口，不承诺自动替换，同时不影响 Windows/Linux 自动更新。
- 0.11.6 的安装包仍包含旧 feed，无法自动获得此修复。用户需手动安装一次 0.11.7；从 0.11.8 开始用递增版本验证 Windows/Linux 自动升级，禁止用同版本覆盖包模拟升级。

### 浏览器生命周期边界

- `BrowserPaneManager` 继续作为公共 Facade，新增 `BrowserTabLifecycle` 管理实例注册去重、关闭中的迟到事件屏蔽/回滚/最终化，以及连续崩溃计数和恢复决策。
- WebContentsView 创建、布局、焦点、扩展、下载、历史/收藏、OAuth 与弹窗仍留在原 Manager，本批未改变外部调用方式或 UI。
- 关闭最后一个窗口不再等同于退出；三端保留应用进程，macOS Dock 和 Windows/Linux 再次启动会恢复现有窗口或按上次工作区创建新窗口，明确选择 Quit 时仍执行完整清理。

### 验证基础设施

- `my-changes` push/PR 触发 Ubuntu 快速验证；Windows/macOS 运行 Electron 类型检查及更新/浏览器生命周期关键测试，不要求每次构建完整三平台安装包。
- 恢复版本一致性、i18n 新缺口、裸 IPC 白名单和 Task/Agent 工具命名检查；30 个既有 i18n 缺口显式记录为基线，新增缺口会失败。
- 文档工具 smoke 由 10 分钟超时包装，超时会明确指出 bundled uv/Python 环境可能卡住；三平台 validate-server 在 `my-changes` push 触发并保留 15 分钟 job 超时。

### 验证

- 更新策略 5 项、BrowserTabLifecycle 6 项、BrowserPaneManager 隔离套件 87 项、Renderer 工作区生命周期 10 项通过。
- Electron 类型检查、i18n parity/sort/coverage、版本、IPC 和工具命名检查通过。
- main、preload、renderer、resources 与 subprocess 正式构建及资源产物校验通过。

## 2026-07-18 v0.11.6 稳定性、跨平台回归与视觉规范

本版本专注稳定性与可诊断性，不引入大型功能或改变 Craft Agents 现有视觉方向。

### 内置浏览器稳定性

- 浏览器标签关闭改为显式状态转换，销毁期间拒绝迟到事件重新插回；关闭失败回滚到主进程权威列表，最后一个标签关闭后安全创建替代标签。
- 并发空白标签创建按本地/远程工作区隔离，避免恢复、新建、连续点击或切换工作区时产生重复标签与错误跳转。
- 新标签页输入由 Renderer 承载，提交导航后才显示原生网页，降低 Windows 中文输入法候选框漂移、焦点争用和空白表面风险。
- 连续页面崩溃且自动恢复耗尽后展示可操作的重新加载界面，并记录崩溃原因和恢复次数。
- 历史、收藏和下载列表改为主进程事件驱动刷新，移除高频轮询；下载进度合并发送，迟到请求不再覆盖当前集合。

### 原生视图、窗口与退出

- Renderer 浮层与 `WebContentsView` 使用带语义原因的暂停协议，Dialog、菜单、Popover、Select、Drawer、通知和最新动态可独立持有并正确释放暂停状态。
- 窗口恢复会按当前显示器可用区域修正旧坐标与尺寸，避免拔除外接屏、虚拟机分辨率或缩放变化后窗口出现在屏幕外。
- 快速重复启动期间的窗口激活与 deeplink 会等待首实例准备完成；退出阶段独立清理浏览器、OAuth、模型刷新、消息网关、电源锁和服务锁，单阶段失败不阻断后续清理。

### 诊断与视觉规范

- 应用设置新增隐私安全的“一键导出诊断包”，汇总版本、平台/架构、Electron/Chromium、代理模式、浏览器崩溃、插件/MCP、启动里程碑和聚合资源占用，不导出原始日志、网址、路径、密钥、PID、命令行或错误正文。
- 建立共享 z-index、控件高度、圆角和安全间距 token，统一浏览器菜单、Popover、Dialog/Drawer、通知、全屏预览、Button、输入框、选择器、Tooltip 和设置卡片的基础边界。
- 修复多个子进程包继承不存在 TypeScript 基准配置的问题，并补齐 Pi runtime、搜索捕获组和响应 Content-Type 的严格空值边界。

### 验证

- Electron、共享运行时、UI、Pi Agent Server 和 Session Tools 类型检查通过。
- 浏览器生命周期、原生浮层协调、窗口恢复、启动激活、退出清理、诊断与资源统计专项回归通过。
- Electron main、preload、renderer、资源与资产正式构建通过；macOS arm64、Windows x64 和 Linux x64 安装包由三平台打包流程生成并进行发布前核验。

## 2026-07-18 v0.11.5 本地优化修复版

### 会话信息面板

- 重构会话「信息」悬浮面板的高度计算：以信息按钮下边缘为固定顶部基准，以当前会话输入框底边为最大高度边界；窗口缩小时同步收缩，内容较少时继续保持自然高度，内容超出后仅在面板内部滚动。
- 输入框底部安全间距统一为共享的 `16px` 布局常量，消除小窗口、系统缩放和悬浮定位二次调整造成的底边偏差。

### 内置浏览器

- 网页中的新窗口链接默认在左侧标签列表创建并激活新标签；按住 macOS `Option` 或 Windows/Linux `Alt` 点击时才打开独立弹窗。
- 关闭 CA 自定义代理时，内置浏览器改为继承 macOS、Windows 和 Linux 系统代理/PAC；代理切换后主动关闭旧连接池，使新路由无需重启即可生效。
- 重构跨平台新标签页输入：空白页搜索框改由 Craft Agents 主界面直接渲染，回车导航后才显示原生 `WebContentsView`，从架构上规避 Windows x64、Windows ARM 虚拟机与 macOS 的焦点争用、输入法飘移、频闪和空白表面问题。
- 修复原生页面获得焦点后重复导航到当前标签、导致浏览器工作区重新挂载并在短暂延迟后失焦的问题。
- 合并并发的空白标签创建请求，避免恢复工作区、新建标签或连续点击时生成重复的「新标签页」。

### 验证

- Electron 类型检查、浏览器与代理专项回归测试、main / preload / renderer 正式构建、资源完整性校验及三平台安装包构建通过。

## 2026-07-18 v0.11.5 浏览器工作区、专注模式与用量信息

- 将 Electron、共享运行时、CLI、无界面服务、WebUI 与内部工作区包版本统一为 `0.11.5`，确保设置页、运行时请求头、安装包和更新清单显示一致。

### 浏览器标签与新标签页

- 停用主浏览器网页上方的旧地址栏工具区，但保留原实现代码以便回滚；导航、刷新、复制链接、收藏、密码、站点权限和扩展入口统一收进当前标签项。
- 新标签页去除装饰图标，仅保留页面正中央的单一输入框；输入网址直接访问，其他文字统一使用 Google 搜索，暂不加入「搜索 / 问 AI」切换。
- 修复浏览器页面覆盖应用菜单、账号菜单、最新动态和弹窗的问题；Renderer 浮层打开时会暂停原生网页视图，关闭后再恢复。
- 修复折叠侧栏与会话列表后网页遮住顶部区域、最新动态与浏览器互相覆盖，以及旧悬浮地址栏残留触发条的问题。
- 标签页「更多」改为卡片内展开，操作时网页不再消失；扩展改为可折叠分组，固定扩展在折叠状态下仍常驻显示并可直接打开。
- 系统通知出现时，原生网页视图仅临时让出通知所占的顶部空间，避免通知被网页覆盖，同时不隐藏整张网页。

### 浏览器扩展

- 扩展安装入口统一为「谷歌商店、微软商店、解压插件、导入插件」四项，并复用插件页的 Craft Agents 菜单样式。
- 支持从 Chrome Web Store 和 Microsoft Edge 扩展商店页面直接安装；商店详情页注入「安装到 Craft Agents」按钮，并补齐商店包下载、解析和安装链路。
- Chrome 商店详情页会隐藏不可用的原生「添加至 Chrome」按钮，并将「添加至 Craft Agents」原位放入页面操作区；无法识别商店布局时仍保留悬浮入口作为回退。
- 扩展列表与详情页补齐扩展图标、名称、版本、说明、权限、来源、启停和卸载信息，改善禁用态与操作反馈。

### 专注模式与本地化

- 左上角布局菜单和 macOS「视图」菜单均新增专注模式入口，并统一提供侧栏、会话列表与全部区域的折叠操作。
- 专注模式下顶部栏自动隐藏，鼠标移到窗口顶部时临时唤醒；macOS 红黄绿窗口按钮同步隐藏，退出专注模式后恢复。
- 进入专注模式时复用右上角通知提示当前平台快捷键与重新打开应用的恢复方式；专注状态不再跨应用重启保存，避免无原生菜单入口的平台无法退出。
- 补齐 macOS 原生文件、编辑、视图、窗口和帮助菜单的中文名称，避免 Electron 系统角色回退成英文；浏览器示例任务与近期新增界面同步汉化。
- Windows 安装包内置并默认使用思源黑体 CN 2.005R 可变 WOFF2，改善中文小字号清晰度且无需单独安装字体；macOS 继续使用系统字体与苹方。

### 会话用量信息

- 会话右上角「来源和输出」入口更名为「信息」，并在来源下方新增「用量信息」。
- 展示当前模型与服务商、上下文已用与剩余比例、输入 / 输出 Token、缓存读取 / 写入 Token 和会话预估费用。
- 费用标签区分官方 API 计价、DeepSeek 标准计价、订阅 / OAuth 和自定义兼容端点；实际账单仍以服务商为准。
- 信息面板改为响应式高度：内容较少时保持自然高度，内容较多时随窗口向下延伸并与输入框底部对齐，仅在超过整段可用空间时滚动。

## 2026-07-18 全量测试稳定性修复

- 补齐插件、浏览器扩展和浏览器打开模式的 RPC 注册声明与远程路由分类，恢复通道穷举校验。
- 修复非 Windows 主机校验 Windows PowerShell 计划文件路径时的盘符、分隔符和大小写处理。
- 修复 CLI TLS 测试证书、分块上传 TTL 时序、WebUI 原生请求和 Electron 模块 mock 的跨测试污染。
- 网络 OAuth 元数据检查改为 `RUN_NETWORK_E2E=1` 显式启用，普通测试不再依赖第三方服务可用性。
- 为德语、西班牙语、匈牙利语、日语和波兰语补齐近期新增界面的英文回退键，保持 locale 结构一致。
- 无界面服务器烟雾测试改用独立临时配置目录，不再与正在运行的 Craft Agents 实例争用锁文件。
- 桌面打包下载 Bun 运行时时加入持久缓存、断点续传、低速检测和多轮重试；网络中断后重新运行会继续未完成的下载，并在解压前验证官方 SHA-256。
- 桌面打包入口扩展为 macOS arm64、Windows x64、Linux x64 AppImage、双平台与三平台模式；成品统一输出到 `~/Downloads/Craft Pack/`，并保留带时间戳的构建日志。
- 修复跨平台打包误带宿主机 ripgrep、koffi、uv 与 Pi 子进程资源的问题；electron-builder 中间目录不再直接污染下载目录，Windows 原生脚本也不再结束其他项目的 Node/Electron 进程。
- 全量测试 4946 项通过、15 项按平台或网络条件跳过、0 项失败；Electron 类型检查通过。

## 2026-07-17 Pi 0.80.10 模型运行时升级

### Pi 与模型

- Pi SDK 从 `0.80.7` 升级至 `0.80.10`，新增 Kimi K3、Kimi Coding HighSpeed、Grok 4.5 Responses 以及最新模型目录元数据。
- Pi 子进程迁移至官方 `ModelRuntime`，主会话、临时 LLM 查询、自定义兼容端点和会话内模型切换共用统一运行时。
- 新增进程内 CredentialStore，保留 API Key、OAuth 热更新和并发刷新串行化，且不向 Pi 默认 `auth.json` 写入凭据。
- GitHub Copilot 登录与刷新改用 provider-owned OAuth；Bedrock IAM 继续通过 AWS 环境和 SigV4 认证。
- 子进程启动禁用逐会话网络模型刷新，继续由 Craft 模型刷新层统一管理目录，避免额外启动延迟和前后端目录不一致。

### 验证

- 新增 Kimi K3/HighSpeed 目录、CredentialStore 并发更新和 `ModelRuntime` SDK 契约测试。

## 2026-07-17 主浏览器工作区与持久化 Runtime

### 浏览器工作区

- 左侧新增「浏览器」主入口，复用桌面端三栏布局：系统导航、浏览标签列表和原生网页区保持与会话工作区一致的结构。
- 中间栏支持标签、收藏、历史和下载四种列表；窄宽度仅显示图标，默认宽度显示图标与文字，不改变原生列表栏宽度规范。
- 浏览标签和当前选中状态写入 Browser Profile，应用重启后恢复标签顺序、地址和活动标签。
- 地址栏支持固定和悬浮两种模式；悬浮模式不挤压网页视口，并修复页面、工具栏、扩展菜单和 Agent 遮罩之间的原生 View 层级冲突。
- 统一网页容器圆角、描边和面板间距，去除重复悬浮遮罩及工具栏显示时的页面位移。

### Profile、登录与网页能力

- 新增独立持久 Chromium Session，保存 Cookie、登录态、缓存、localStorage、IndexedDB 和站点权限，用户浏览与 Agent 操作共用同一标签环境。
- 接入浏览历史、收藏夹、文件夹管理、书签导入导出、下载进度、暂停、继续、重试和 Finder 定位。
- 支持网页弹窗、OAuth 登录窗口、子窗口生命周期和安全协议过滤；浏览器 User-Agent 移除 Electron 标识以改善站点兼容性。
- 完善导航、表单填写、页面读取、截图、选择、滚动和脚本执行桥，使现有 Browser Runtime 可直接控制主浏览器标签。

### 浏览器扩展

- 插件工作区新增「智能 / 浏览」切换，将 Agent 插件与浏览器扩展分开管理。
- 支持从 Chrome Web Store 链接或扩展 ID 下载并解析 CRX3，也支持 CRX / ZIP 包和已解压目录安装。
- 扩展复制到 CA 管理目录后加载，支持重启恢复、启停偏好、固定、隐藏、排序、弹出页 / 选项页和卸载。
- 扩展弹层使用原生覆盖层并保持在网页上方；权限升级和安装失败会给出明确提示。
- 基于 Electron Chromium 扩展 API 兼容子集运行，依赖 Chrome 专有 API 的扩展可能仅部分可用。

### 密码与系统认证

- 新增浏览器密码仓库：macOS 正式签名构建优先写入 Data Protection Keychain / iCloud Keychain，其他环境回退到 Electron 本机加密存储。
- 登录表单提交后可保存或更新凭据；再次访问同源页面时选择账号，通过 Touch ID 后自动填充用户名和密码。
- 密码菜单支持按站点查看账号、Touch ID 填充和删除，Renderer 与网页脚本不会直接持有完整密码列表。
- 打包流程新增 Swift Keychain helper、浏览页面 preload、Touch ID WebAuthn keychain group 和资源完整性校验。

### 验证

- 新增 Browser Profile、密码仓库、路由、IPC 和 Browser Runtime 专项测试，共 99 项通过。
- Electron 类型检查、生产 renderer / preload / main / resources 构建和资源校验通过。
- macOS arm64 成品 App 已完成生成、ad-hoc 深度签名结构校验和隔离配置启动冒烟测试；正式签名与公证留待发布阶段执行。

## 2026-07-15 v0.11.4 五层扩展协议与插件市场

### 扩展协议

- 补齐可移植 Skill、插件包、MCP 工具、交互式 Widget 与宿主权限五层扩展协议，并加入对应的生命周期、诊断、恢复和安全边界。
- 支持 `.craft-plugin`、`.codex-plugin` 与 `.claude-plugin` 插件清单，统一发现 Skill、MCP、App 和 Widget 资源。
- 新增插件级启停、工具权限策略、MCP 健康诊断、会话资源回收与安全恢复。
- 扩展 Skill 元数据兼容性，支持项目目录向上发现、`agents/openai.yaml` 以及更宽容的列表字段解析。

### 插件与市场

- 左侧栏新增「插件」工作区，支持 Git 安装、本地添加、启停、卸载、工具策略和 MCP 状态查看。
- 新增插件市场浏览器，默认接入公开的 `openai/plugins` 市场，并支持搜索、刷新、添加或移除其他 Git marketplace。
- 市场安装使用稀疏检出与本地缓存，仅拉取清单和选中的插件目录，并对路径穿越和符号链接逃逸进行校验。
- 对依赖 OpenAI Connector 或外部账号的插件显示兼容提醒，不支持的 NPM marketplace 包会明确标记。
- 插件内部 Skill 在技能页按插件归档为可展开分组，`@` 菜单只显示一个插件总入口，并保留品牌图标。
- 安装、启停、卸载插件后可热刷新技能菜单与 MCP 工具，活动会话在下一轮安全更新。
- 修复插件市场详情栏被挤压、首次安装缺少目标目录导致 `ENOENT` 的问题。

### 账号与认证

- 设置新增「账号」页面，用于外部账号绑定认证；Google OAuth 应用凭据进入本地 AES-256-GCM 加密仓库。
- Gmail、Google Calendar、Google Drive 等插件连接器可映射到 CA 原生数据源，并复用统一 OAuth、刷新与权限链路。
- 插件详情展示未连接、已连接、已过期、配置缺失、宿主未批准和不支持状态，并提供重连与断开操作。
- MCP OAuth 不再在动态注册被拒后冒用其他宿主客户端；服务商限制会持久记录并明确展示。
- 修复动态注册返回的 `client_secret` 在 token 交换与刷新阶段被丢弃的问题。

### 界面体验

- 插件和 Skill 支持包内品牌图标及语义化回退图标，并同步显示在列表、详情、`@` 菜单和消息中。
- 右侧审阅栏默认显示浏览器、终端、文件夹和 Cowart 启动入口，不再自动创建空浏览器标签。

### MCP Apps 与 Widget

- 新增通用 MCP Apps Widget Host，可在消息内或右侧审阅栏渲染插件交互界面，不再为 Canvasight 等应用逐个制作专用适配。
- 支持 `structuredContent`、`_meta`、MCP 资源、CSP、主题同步、工具调用、后续消息回传与最小 `window.openai` 兼容桥。
- Widget 工具调用继承 Explore、Ask、Execute 会话权限，并加入确认、插件策略、重连、错误重试和会话关闭清理。

### Pi 与构建

- Pi 运行时升级至 `0.80.7`，加入会话与连接池级动态工具目录；工具启停可原地更新，Schema 变化时安全重建会话。
- 强化 DMG/EXE 打包资源验证，确保 Electron 主进程、preload、renderer、插件与 Widget 运行资源完整。

## 2026-07-15 对话内可视化与 Cowart Widget 宿主

### 新增

- 新增 Codex-compatible Inline Visualization Runtime，可识别回答中的 `::codex-inline-vis` 指令并直接渲染交互式 HTML 组件。
- 内置 Visualize 技能及完整资源包，安装应用后即可生成图表、模拟器、数据探索器和交互工具，无需单独复制 Skill。
- 新增 Widget Host、主题切换、自适应高度、Lucide 图标、Tooltip 和 `window.openai.sendFollowUpMessage` 对话回传能力。
- 新增 Cowart Widget 打开链：Agent 调用 `render_cowart_canvas_widget` 后，可自动在右侧审阅栏打开绑定当前项目和会话的画布标签页。
- 新增本地与远程会话统一的 Widget 文件读取接口，支持从当前项目的 `.craft/visualizations/` 安全加载可视化文件。

### 兼容与安全

- Visualize 内部样式和交互运行时直接复用 Codex 原版资源；Craft Agent 仅负责外层容器、主题模式、安全沙箱和确认弹窗。
- iframe 禁用 Node、同源权限、弹窗、顶层导航和嵌套页面，并通过 CSP 限制脚本、资源和网络访问。
- Widget 文件读取限制在当前会话允许的工作目录，拒绝路径穿越、越界绝对路径和符号链接逃逸。
- Cowart 本地服务加入项目目录校验、运行实例 ID、页面 ID 校验、启动就绪检测和可信回传校验。

### 技能与构建

- 新增内置技能来源层级：内置技能优先级最低，可继续由全局、空间或项目中的同名技能覆盖。
- 技能列表为内置技能显示只读来源状态，并补齐全部界面语言文案。
- 新增打包资源验证脚本，逐文件校验 `resources/` 与 `dist/resources/`，并检查 Electron、preload、renderer 和终端必需产物。

### 修复

- 修复流式与非流式回答走不同 Markdown 渲染路径时，内联可视化只能在部分消息中显示的问题。
- 修复完整 HTML 与 HTML 片段在 Widget 容器中重复嵌套文档的问题。
- 修复可视化内容高度超过旧上限后被截断，以及主题切换后组件未同步更新的问题。
- 修复内联可视化根页面强制显示白色背景的问题，使外层画布自动匹配 Craft Agent 当前主题，同时保留可视化自身的内容样式。
- 移除 Visualize 宿主默认添加在生成内容外围的根级内边距，使交互内容与消息内容区边缘自然衔接。

## 2026-07-14 审阅栏、浏览器与 Cowart 集成优化

### 新增

- 右侧审阅栏改为标签页模式，支持在「审阅」标题后直接打开浏览器、终端、文件夹、Cowart 画布和来源页。
- 新增 Cowart 画布入口，可在右侧审阅栏启动本地 Cowart 服务并打开 `127.0.0.1:43217` 画布页面。
- 新增来源详情标签页：会话资源面板中的来源超过 3 项时显示「查看全部」，点击后在右侧审阅栏打开完整来源列表。
- 新增左上角折叠布局菜单，支持折叠侧栏、折叠会话列表和折叠全部三种布局。

### 改版

- 重做右侧浏览器标签栏和地址栏样式，去掉多余内嵌边框，未聚焦时显示简化域名，聚焦时显示完整地址输入框。
- 右侧浏览器工具栏改为可悬浮 / 可固定：未固定时向上隐藏以扩大网页内容区，固定后保留占位避免遮挡网页。
- 对话内容定位器移动到输入区右侧「信息」按钮旁，改为横向刻度条，悬停时展示当前片段摘要、文件和来源信息。
- 会话资源面板中的标题与会话文件信息从右侧审阅默认视图中移除，避免与悬浮资源卡片重复。

### 修复

- 修复主对话区与右侧审阅栏之间 resize 拖拽黏滞、松手后仍保持拖拽状态的问题。
- 修复右侧审阅栏最小宽度过大，导致主对话区和审阅区在小窗口下无法灵活调整的问题。
- 修复右侧浏览器地址栏输入 URL 后回车无响应的问题。
- 修复右侧浏览器、终端、文件夹等工具被当作固定窗口展示的问题，统一为审阅栏标签页。
- 修复对话定位器默认状态常驻 hover 波峰的问题，改为默认等高，仅在悬停时按中心向两侧递减。

## 2026-07-13 界面、审阅与资源面板改版

### 新增

- 新增右侧审阅栏，支持在会话旁边查看信息、工作区文件、内嵌终端和浏览器控制面板。
- 新增内嵌终端窗口与审阅栏终端视图，支持创建、输入、resize、关闭和进程退出回传。
- 新增会话资源悬浮面板，统一展示标题、上下文、会话文件、输出和来源。
- 新增对话内容定位器，在聊天区左侧显示轻量刻度，滚动时同步当前片段，悬停时展示标题与内容摘要。
- 新增会话列表轻量 hover 摘要卡，显示会话标题、摘要、空间 / 工作目录和 Git 分支。

### 改版

- 将原会话信息弹窗中的核心数据复制到会话资源悬浮面板中，信息入口保留但不再作为主要操作路径。
- 将“来源 +”接入底部输入框已有来源选择器，复用原 Craft 样式和选择逻辑。
- 将“输出 +”接入当前输入框，用于快速生成当前会话的可交付输出文件或站点。
- 右侧审阅栏默认展示信息视图，并保留文件、终端、浏览器快速切换。
- 聊天主区域在资源面板打开时自动让出宽度，避免悬浮面板压住消息卡、滚动条或输入区。

### 优化

- 优化资源悬浮面板布局，使其靠近右上角工具按钮，并与关闭按钮区域保持视觉对齐。
- 优化对话定位器样式：默认刻度更细、更短、更轻，选中态更克制，并加入宽度、颜色和透明度过渡。
- 优化消息列表 hover 预览，不再加载完整消息和会话文件，避免展示标题、文件和大量消息摘要造成干扰。
- 优化设置页 Messaging 内容区宽度，补齐左右留白，与其他设置页保持一致。
- 优化中文设置列表文案：`Workspace` 改为「空间」，`Messaging` 改为「消息」，`AI` 改为「模型」，消息副标题改为「连接移动设备」。
- 优化浏览器打开策略，可按设置在右侧栏或独立窗口中打开。

### 修复

- 修复会话资源面板与主聊天区重叠的问题。
- 修复右侧栏展开时主对话内容没有跟随宽度适配的问题。
- 修复会话资源面板点击外部即关闭的问题，使面板保持可持续操作。
- 修复来源和输出入口未接入实际交互的问题。
- 修复消息设置页内容区贴边的问题。

### 构建与协议

- 补齐终端相关 Electron IPC、preload API、通道映射和类型定义。
- 补齐右侧审阅栏相关路由、导航状态、面板容器和设置项。
- 打包流程复制终端页面、终端 preload 和运行时资源，确保发布包可用。

## 2026-07-12 历史对话滚动位置

### 新增

- 新增「设置 → 输入 → 历史对话」设置项，可选择打开对话时的滚动位置：
  - 底部：始终定位到最后一条消息。
  - 顶部：从对话历史顶部开始。
  - 上次位置：恢复到上次浏览位置。

### 修复

- 修复打开历史会话时默认停在顶部的问题。
- 修复图片、代码块和懒渲染 turn 尚未完成布局时，初始滚动位置计算不准确的问题。
- 修复自动滚动补偿与流式跟随之间的冲突，使用户手动上滑时不会被强行拉回底部。

### 配置

- 补齐 `config-defaults`、配置 schema、存储读写、RPC handler、通道映射、ElectronAPI 类型和设置页 UI。
- 补齐多语言文案。

## 2026-07-12 同步 upstream v0.11.1

### 同步

- 同步 OpenAI GPT-5.6（Luna、Terra、Sol）连接支持。
- 同步模型原生 Max thinking level。
- 升级 Pi SDK 至 0.80.6。
- 改进长上下文成本统计。

### 修复

- 修复 Electron renderer 的工具结果事件类型。
- 修复输入框属性相关类型检查问题。

## 2026-07-09 本地打包与上游问题修复

### 修复

- 修复 macOS 中文输入法场景下自动大写干扰输入的问题。
- 修复 Opus 4.8 / 4.7 模型描述重复的问题。
- 修复编辑连接时遮罩 API Key 被误作为真实凭证回传的问题。
- 修复生产环境 auto-update 诊断日志不可见的问题。
- 修复 `file://` 本地链接被 URL 安全分类拦截的问题。
- 修复 Skill 目录为符号链接时无法识别的问题。
- 修复 Write 工具覆写已有文件时 diff 只显示全量新增的问题。
- 修复打包后缺少 SDK 或子进程资源导致启动失败的问题。
- 修复 macOS Dock 图标在深色模式下被固定图标覆盖的问题。

### 构建

- macOS 打包改走 `build-dmg.sh`，自动复制 SDK 与子进程资源。
- Windows 打包补齐 SDK core、win32-x64 binary 和 binary alias。
- Electron 打包保留经典 `icon.icns`，避免 Liquid Glass 图标渲染不一致。

## 2026-07-08 初始整理

### 新增

- 基于 upstream v0.11.0 建立个人本地化分支。
- 新增微信 ilink 协议适配器，支持扫码登录、多账号、消息收发、媒体处理和会话同步。
- 新增消息平台配置与会话绑定相关前端入口。
- 新增本地打包脚本与资源复制流程。

### 仓库

- 从 fork 形态调整为独立仓库，避免 GitHub LFS 对象上传限制。
- 主开发分支为 `my-changes`。
