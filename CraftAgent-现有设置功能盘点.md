# CraftAgent 现有设置功能盘点

审查基准：当前代码可达性（`settings-registry.ts` / `menu-schema.ts` / `SettingsNavigator.tsx` / 各 Settings 页 + IPC/存储）。本轮只盘点，不改代码。

---

## 1. 设置页面总览

侧栏正式可见页面：**14 个**（`FORMAL_SETTINGS_PAGES`）。  
注册表共 16 个：另有 `browser`（组件完整，侧栏漏显）、`cognition`（诊断深链，刻意隐藏）。

| 设置页面 | 侧栏标签（zh-Hans） | 主要功能 | 完整度 | 是否建议保留 |
|---|---|---|---|---|
| 应用 `app` | 通用 | 通知、防休眠、内置浏览器开关、代理、诊断导出、关于/更新 | 完整可用 | 是 |
| 书签 `bookmarks` | 书签 | 浏览器书签增删与打开 | 完整可用 | 是 |
| 浏览器 `browser` | （侧栏不可见） | 链接打开、新标签、缓存/站点数据、下载、权限例外 | 功能完整，入口缺失 | 是（需补入口） |
| 模型 `ai` | 模型 | LLM 连接、默认模型/思考、工作区覆盖、性能（1M/缓存/RTK） | 完整可用 | 是 |
| 用户记忆 `preferences` | 用户记忆 | 本地称呼名、时区、地点、自由笔记 | 可用（仅部分 preferences 字段） | 是 |
| 外观 `appearance` | 外观 | 主题/字体/语言、工作区主题与头像色、玻璃、看板、工具图标 | 完整可用 | 是 |
| 输入 `input` | 输入 | 自动大写、拼写、发送键、会话滚动、右侧栏、浏览器打开位置 | 完整可用 | 是 |
| 快捷键 `shortcuts` | 快捷键 | 快捷键参考 + 双 Command/Alt 截图开关 | 半参考半可用 | 是 |
| 标签 `labels` | 标签 | 标签树、自动规则 | 完整可用 | 是 |
| 空间 `workspace` | 空间 | 工作区名称/图标、默认权限模式、模式循环、默认 Sources、工作目录、Local MCP | 完整可用 | 是 |
| 服务器 `server` | 高级（侧栏改名） | 远程 Server、端口、TLS、Token | 完整可用 | 是 |
| 账号 `accounts` | 账号 | Google OAuth **应用** Client ID/Secret | 可用，但文案易误解 | 是（建议改名，非账号体系） |
| 权限 `permissions` | 权限 | Explore/safe 白名单规则表（默认 + 工作区） | 完整可用 | 是 |
| 隐私 `privacy` | 隐私 | 上下文感知、隐私模式、来源权限、清理、访问日志 | 基本完整 | 是 |
| 消息 `messaging` | 消息 | 飞书/微信连接与绑定 | 完整可用 | 是 |
| 认知 `cognition` | （隐藏） | Cognition store 诊断 | 调试页 | 是（保持隐藏） |

---

## 2. 现有设置功能

### 2.1 应用（`app`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 通知 | 桌面通知 | 任务完成等桌面通知 | 开关 | 是 → `config.json` | 正常使用 |
| 电源 | 运行时保持唤醒 | 会话运行时防休眠 | 开关 | 是 → `config.json` + 主进程 | 正常使用 |
| 工具 | 内置浏览器 | 启用/禁用内置浏览器工具 | 开关 | 是 → `config.json` | 正常使用（与浏览器页重复） |
| 网络 | HTTP/HTTPS 代理 | 代理开关与地址、绕过规则 | 开关 + 输入 + 保存 | 是 → `config.json` | 正常使用 |
| 诊断 | 导出诊断 | 导出诊断包（仅 Electron） | 按钮 | 是 | 正常使用 |
| 关于 | 版本 / 检查更新 / 安装更新 | 版本显示与自动更新 | 展示 + 按钮 | 是 | 正常使用 |

主要文件：`AppSettingsPage.tsx`；存储：`~/.craft-agent/config.json`。

### 2.2 书签（`bookmarks`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 添加书签 | URL | 添加浏览器书签 | 输入 + 按钮 | 是 | 正常使用 |
| 已保存 | 书签列表 | 打开/删除书签 | 列表 + 按钮 | 是 | 正常使用 |

存储：Electron `userData/browser-profile/profile.json`。

### 2.3 浏览器（`browser`，深链可达）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 通用 | Agent 使用内置浏览器 | 同应用页 browser tool | 开关 | 是 | 正常使用（重复） |
| 通用 | 链接打开方式 | 内部 / 系统 / 询问 | 下拉 | 是 | 正常使用 |
| 通用 | 新标签行为 | 默认 / 空白 / 自定义 URL | 下拉 + 编辑 | 是 | 正常使用 |
| 浏览数据 | 清除数据 / 缓存 / Cookie | 清除与按站点管理 | 按钮 + 对话框 | 是 | 正常使用 |
| 下载 | 下载位置 / 每次询问 / 历史 | 下载路径与历史入口 | 按钮 + 开关 | 是 | 正常使用 |
| 权限 | 站点权限行为与例外 | ask/block + 例外列表 | 下拉 + 列表 | 是 | 正常使用 |
| 高级 | 重置浏览器设置 | 恢复默认 | 按钮 | 是 | 正常使用 |

主要文件：`BrowserSettingsPage.tsx`；存储：`profile.json` + `config.json.browserToolEnabled`。  
**侧栏无入口**（`FORMAL_SETTINGS_PAGES` 未含 `browser`）。

### 2.4 模型（`ai`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 默认 | 连接 / 模型 / 思考级别 | 应用级默认 | 下拉 | 是 | 正常使用 |
| 工作区覆盖 | 每工作区连接/模型/思考 | 覆盖应用默认 | 可展开卡片 | 是 → workspace `config.json` | 正常使用 |
| 连接 | 增删改、校验、设默认、中途发送行为 | LLM 连接管理（含 OAuth） | 列表 + 菜单 | 是 → `config.json` + `credentials.enc` | 正常使用 |
| 性能 | 1M 上下文 / 扩展 Prompt Cache / RTK | 模型性能相关开关 | 开关 / 按钮 | 是 → `config.json` | 正常使用 |

主要文件：`AiSettingsPage.tsx`。

### 2.5 用户记忆（`preferences`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 基本信息 | 名称 | Agent 如何称呼你（本地） | 输入 | 是 → `preferences.json` | 正常使用 |
| 基本信息 | 时区 | 用户时区 | 输入 | 是 | 正常使用 |
| 位置 | 城市 / 国家 | 本地位置信息 | 输入 | 是 | 正常使用 |
| 笔记 | 自由笔记 | 写入系统提示的用户记忆 | 文本域 | 是 | 正常使用 |

主要文件：`PreferencesPage.tsx`。  
未暴露但存在于 `preferences.json`：`explore.*`、`diffViewer`、`includeCoAuthoredBy`、`privacy`、`uiLanguage` 等。

### 2.6 外观（`appearance`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 默认主题 | 模式 / 色主题 / 字体 / 语言 | 全局外观与 UI 语言 | 分段 / 下拉 | 是 | 正常使用 |
| 工作区主题 | 每工作区色主题 + 头像底色 | 主题覆盖与列表头像色 | 下拉 + 取色 | 是（主题→workspace；色→localStorage） | 正常使用 |
| 界面 | 连接图标 / 富工具描述 / 后台完成芯片 / 玻璃强度 / 项目色条 | 界面细节 | 开关 / 下拉 | 是 | 正常使用 |
| 看板 | 列颜色 / Live Pulse | Kanban 外观 | 取色 + 开关 | 是 → localStorage | 正常使用 |
| 看板状态 | 拖入列时应用状态 | 列→状态映射 | 下拉 | 是 | 正常使用（放在外观略偏） |
| 工具图标 | CLI→图标映射表 | 展示与编辑配置文件 | 表格 + EditPopover | 是 | 正常使用 |

主要文件：`AppearanceSettingsPage.tsx`。

### 2.7 输入（`input`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 输入 | 自动大写 | 输入首字母大写 | 开关 | 是（`FreeFormInput`） | 正常使用 |
| 输入 | 拼写检查 | textarea spellCheck | 开关 | 是 | 正常使用 |
| 发送 | 发送快捷键 | Enter / Cmd+Enter | 下拉 | 是 | 正常使用 |
| 历史会话 | 打开滚动位置 | 底/顶/上次 | 下拉 | 是 | 正常使用 |
| 右侧栏 | 模式 / 跟随会话 / 浏览器打开位置 | 侧栏与浏览器布局 | 下拉 + 开关 | 是 | 正常使用 |

存储：`~/.craft-agent/config.json`。

### 2.8 快捷键（`shortcuts`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 屏幕截图 | 双 Command/Alt 截图 | 截图进输入框 | 开关 | 是 | 正常使用 |
| 屏幕截图 | 截图时隐藏应用 | 截前隐藏窗口 | 开关 | 是 | 正常使用 |
| 屏幕截图 | 系统权限 / 重新检测 | 辅助功能与录屏权限 | 状态 + 按钮 | 是 | 正常使用 |
| 各分类 | 快捷键一览 | 只读参考 | 展示 | 展示层 | 正常使用（不可编辑） |

主要文件：`ShortcutsPage.tsx`。

### 2.9 标签（`labels`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 标签层级 | 创建/删除标签 | 工作区标签树 | 表格 + 内联创建 | 是 → `labels/config.json` | 正常使用 |
| 自动规则 | Auto-apply 规则 | 正则自动打标 | 表格 + EditPopover | 是 | 正常使用 |

### 2.10 空间（`workspace`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 工作空间信息 | 名称 | 当前工作区显示名 | 编辑对话框 | 是 → workspace `config.json` | 正常使用 |
| 工作空间信息 | 图标 | 本地头像文件 | 文件上传 | 是 → `icon.*` | 正常使用 |
| 权限 | 默认模式 | safe/ask/allow-all | 下拉 | 是 | 正常使用 |
| 模式循环 | 可循环模式 | Shift+Tab 可切哪些模式 | 多开关 | 是 | 正常使用 |
| 默认 Sources | 默认启用的 Sources | 新会话默认资源 | 开关列表 | 是 | 正常使用 |
| 高级 | 默认工作目录 | 会话默认 cwd | 目录选择 | 是 | 正常使用 |
| 高级 | Local MCP | 是否允许本地 stdio MCP | 开关 | 是 | 正常使用 |
| 高级 | 插件包管理 | 跳转插件页 | 按钮（硬编码中文） | 是（导航） | 正常使用 |

### 2.11 服务器（`server`，侧栏显示为「高级」）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 远程访问 | 启用 Server 模式 | 允许远程连接本机 | 开关 | 是 → `config.json.serverConfig` | 正常使用 |
| 连接 | 端口 / URL / Token / TLS | 远程访问配置 | 输入 + 浏览 | 是；启用变更需重启（有提示） | 正常使用 |

### 2.12 账号（`accounts`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| Google OAuth | Client ID / Secret | **外部集成用 OAuth 应用凭证**，非用户登录 | 输入 + 保存/删除 | 是 → `credentials.enc` | 正常使用 |

说明：页面名「账号」易误解为产品账号体系。

### 2.13 权限（`permissions`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 说明 | 权限机制说明 | 文档说明 | 文本 | — | 正常使用 |
| 默认权限 | 全局白名单 | `~/.craft-agent/permissions/default.json` | 表格 + 编辑文件 | 是 | 正常使用 |
| 工作区自定义 | 工作区追加规则 | `{workspace}/permissions.json` | 表格 | 是 | 正常使用 |

与「空间 → 默认模式」互补：一个选模式，一个配 safe 白名单。

### 2.14 隐私（`privacy`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 全局 | 上下文感知 / 隐私模式 | 总开关与隐私模式 | 开关 | 是 | 正常使用 |
| 自动上下文 | 会话活动 / 浏览器活动 / 项目活动 | 后台来源 allow/off | 分段控件 | 是 | 正常使用 |
| 交互访问 | 会话内容 / 归档 / 资源库生成 | allow/ask/deny | 分段控件 | 是 | 正常使用 |
| 敏感内容 | 说明文案 | 固定开启，不可改 | 纯文本 | 无控件（设计如此） | 仅 UI |
| 清理 | 存储用量 / 清认知 / 清日志 | 清理本地隐私数据 | 按钮 | 是 | 正常使用 |
| 透明度 | 访问日志 | 最近访问记录 | 列表 | 是 | 正常使用 |

### 2.15 消息（`messaging`）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 平台 | 飞书 / 微信 | 连接、重配、断开、会话绑定、访问控制 | 卡片 + 对话框 | 是 → workspace `messaging/` | 正常使用 |

### 2.16 认知（`cognition`，隐藏）

| 分组 | 设置项 | 功能说明 | 控件 | 是否生效 | 状态 |
|---|---|---|---|---|---|
| 诊断 | Events/Observations/Loops 等 | Cognition 调试查看与清理 | Tab + 按钮 | 是 | 半成品（调试） |

---

## 3. 本地资料与 Workspace

### 头像

- **工作区图标文件**：`WorkspaceSettingsPage` 上传 → `{workspaceRoot}/icon.{png,jpg,jpeg,svg,webp,gif}`
- **工作区头像底色**：`AppearanceSettingsPage` → renderer `localStorage`（`workspaceAvatarColorsAtom`）
- 使用范围：工作区切换器、侧栏、设置页、AI 覆盖卡片等（`WorkspaceAvatar` / `useWorkspaceIcon`）

### 名称

| 名称 | 保存位置 | 用途 |
|---|---|---|
| Workspace 名称 | `{workspaceRoot}/config.json` → `name` | 工作区身份展示 |
| 用户记忆「名称」 | `~/.craft-agent/preferences.json` → `name` | Agent 如何称呼你（写入提示词） |

二者独立，**都不是账号**。

### Workspace 信息

- 配置：`{workspaceRoot}/config.json`（名称、默认模式、Sources、工作目录、主题覆盖、Local MCP 等）
- 注册：全局 `~/.craft-agent/config.json` 的 workspaces 列表

### 是否与账号概念混淆

- 产品无登录/注册/云端个人资料。
- `Accounts` 中文标题为「账号」，实际是 Google OAuth **应用凭证**。
- `Preferences`「名称」是本地用户记忆，不是账号显示名。

**结论：CraftAgent 当前为纯本地应用，本地头像和名称不属于账号体系。**

---

## 4. 存在的问题

1. **浏览器设置入口缺失**：`BrowserSettingsPage` 完整，且 Navigator sections 含 `browser`，但 `FORMAL_SETTINGS_PAGES` 未包含 → 侧栏/App 菜单不可见；仅深链 `settings/browser` 可达。
2. **重复设置**：`browserToolEnabled` 同时出现在「应用 → 工具」与「浏览器 → 通用」。
3. **文案易误解为账号**：`settings.accounts.title`（zh-Hans「账号」）实际为外部 OAuth 应用凭证；英文也是 “Accounts & authentication”。
4. **侧栏命名不一致**：`server` 页在侧栏显示为「高级」（复用 `settings.workspace.advanced`），与页面标题「服务器」不一致；`app` 侧栏显示为「通用」。
5. **两处「名称」**：用户记忆名称 vs Workspace 名称，语义不同，界面都叫名称。
6. **隐私「敏感内容」分组仅说明**：无控件（文案写明 always on），易被当成半成品。
7. **快捷键页大部分只读**：不可自定义快捷键；可交互的主要是截图开关。
8. **用户记忆页未覆盖全部 preferences**：如 `includeCoAuthoredBy`、任务提醒/`explore.*` 等无设置入口（部分可由 Agent 工具改）。
9. **旧深链重定向过时**：`route-parser` 仍把顶层 `privacy/labels/messaging/server` 重定向到其他页，与当前正式设置页并存冲突风险。
10. **空间页「插件包管理」硬编码中文**，未走 i18n。
11. **看板列状态映射放在外观页**：功能生效，但更像看板行为而非外观。
12. **需重启提示**：目前仅 Server 启用变更有重启横幅；未发现其它设置改完需重启却无提示的情况。

未发现 Craft 账号登录/注册/云同步设置页残留。LLM OAuth、飞书/微信连接属于集成认证，不是产品账号。

---

## 5. 底层存在但设置页缺少入口的能力

### 会话
- 会话级 model / thinking / permissionMode / cwd 等存在于 session 数据；无独立「会话设置」页（合理，多数在聊天 UI）。
- 任务提醒、`explore.*` 偏好有存储与逻辑，设置中无入口。

### 浏览器
- 完整 Browser 设置页存在，但正式菜单无入口（见上）。
- `allowRemoteEvaluate`（远程 evaluate）在 `config.json`，无 UI。

### 项目
- `{workspace}/projects/{slug}/config.json`（名称、目录、看板列、颜色等）在项目 UI 编辑，无 Settings 页。

### 自动化
- `{workspace}/automations.json` 等在自动化编辑器管理，无 Settings 页。

### 技能
- `{workspace}/skills/` 在技能面板管理，无 Settings 页。

### 资源库
- knowledge/library 本地存储存在；隐私页有「资源库生成」权限，但无资源库专属设置页。

### 隐私与记忆
- 隐私页已覆盖策略主路径。
- Cognition 调试页隐藏；正式清理入口在隐私页。
- `includeCoAuthoredBy` 无设置 UI（可由偏好工具写入）。

### 模型与连接
- AI 设置页覆盖较全。
- 插件配置在插件中心；空间页仅提供跳转。

---

## 6. 简要结论

- **已覆盖**：应用通用、AI/连接、用户记忆、外观、输入、快捷键参考、标签、工作区身份与默认行为、权限规则、隐私策略、消息集成、远程 Server、书签；浏览器能力代码齐全。
- **最明显缺少**：浏览器设置正式入口；部分本地偏好（提醒、Co-Authored-By、远程 evaluate）无 UI；项目/自动化/技能/资源库以功能面板管理而非 Settings。
- **建议先清理**：补上或删除 `browser` 的 FORMAL 漏网；理顺「账号」命名；消除 browser tool 双入口；校正 server 侧栏「高级」标签；收敛旧深链重定向注释/逻辑。
- **适合下一步与 Codex 设置交叉比对**：当前结构清晰、以本地配置为主、无账号云同步包袱；比对时应聚焦「本地能力组织与命名」，不要引入账号体系对照。
