# CraftAgent 原生 UI 全量核对与修改说明

> 用途：基于前两轮 UI 修复，重新完整跑一遍桌面端布局、排版、样式、交互与功能。  
> 核对日期：2026-07-27  
> 核心原则：优先复用现有原生组件和成熟页面，不为相同交互另造第二套 UI。

## 1. “原生 UI”在本项目中的定义

以下现有页面和组件是本轮核对基准，不以个人感觉重新设计：

| 场景 | 原生基准 | 必须保持一致的内容 |
|---|---|---|
| 三栏页面 | 会话页面 | 左侧主导航、中间列表、右侧详情的宽度关系、分隔、滚动归属 |
| 列表项 | 会话列表 `EntityRow` | 行高、左右留白、图标、标题、副标题、选中背景、左侧指示、悬浮和菜单 |
| 页面标题 | `PanelHeader` | 标题位置、右侧图标按钮、分隔、拖拽区和操作密度 |
| 标题菜单 | 会话/浏览器标题右侧菜单 | 搜索、筛选、排序和低频操作集中放入同一菜单 |
| 右键/下拉菜单 | `StyledContextMenu` / `StyledDropdownMenu` | 圆角、阴影、内距、禁用态、危险操作色、层级 |
| 图标按钮 | `HeaderIconButton`、`Button` | 尺寸、圆角、悬浮、按下、禁用、提示文本 |
| 自然语言创建 | 会话输入框式创建面板 | 大画布、居中说明、底部输入框、Enter 提交、Shift+Enter 换行 |
| 详情页 | `Info_Page` / 原 `SkillInfoPage` | Hero、元数据、分区、表格、说明、权限与右上角操作 |
| 设置页 | 现有 Settings 组件 | 分组、卡片、行高、说明文字、开关、状态和操作按钮 |
| 空状态 | `EntityListEmptyScreen` | 图标、标题、说明、唯一主操作，不能再做独立样式 |

出现以下任一情况，直接判定为“没有使用原生 UI”：

- 相同功能出现两套不同弹窗、按钮或菜单。
- 页面自己写圆角卡片、阴影、选中态，而现有组件已经能完成。
- 搜索、筛选、排序散落在正文或重复显示，没有收进标题菜单。
- 左侧选择后，右侧只出现临时摘要卡，而不是完整详情。
- 创建流程要求用户填写内部字段、ID、模型名、提示词等技术配置。
- 页面导航切换后，左上角“新建”名称、标题或列表内容没有同步变化。
- 原生浏览器层遮挡菜单、弹窗、通知或其他应用界面。

## 2. 全局布局核对

### 2.1 三栏结构

- [ ] 会话、浏览器、项目、自动化、知识库、动态、技能、设置均检查是否遵循各自应有的栏数。
- [ ] 需要三栏的页面必须是：左侧主导航 / 中间对象列表 / 右侧对象详情。
- [ ] 中间列表不能空着占位，不能显示其他模块的遗留标题。
- [ ] 右侧未选择对象时，只显示统一空状态，不显示假详情或无意义卡片。
- [ ] 每一栏独立滚动；页面整体不能出现多余的横向滚动。
- [ ] 调整窗口宽度时，三栏按既有紧凑模式收缩，不重叠、不截断关键操作。
- [ ] 原生浏览器视图只出现在浏览器内容区，切换到其他模块后不可见、不可拦截点击。

### 2.2 标题区

- [ ] 中间栏标题必须对应当前模块：所有会话、所有标签、所有项目、所有自动化、知识库、动态、所有专家/技能/连接器、设置。
- [ ] 标题下不再重复出现同义标题、副标题或说明。
- [ ] 右侧图标按钮使用统一尺寸、间距、圆角和悬浮态。
- [ ] 搜索、筛选、排序、导入等低频操作优先进入标题右侧菜单。
- [ ] 标题菜单打开后不能被浏览器原生层、右侧面板或其他内容覆盖。

### 2.3 左上角动态“新建”

逐项切换左侧主导航，核对按钮文字和行为：

| 当前模块 | 必须显示 | 点击结果 |
|---|---|---|
| 未明确选择/会话 | 新建会话 | 创建新会话 |
| 浏览器 | 新建网页 | 创建并选中新标签页 |
| 项目 | 新建项目 | 打开自然语言创建项目 |
| 自动化 | 新建自动化 | 打开自然语言创建自动化 |
| 知识库 | 新建文档 | 使用知识库文档创建流程 |
| 动态 | 新建会话或无创建动作 | 按产品定义，不得显示错误模块文字 |
| 技能-专家 | 新建专家 | 打开自然语言创建专家 |
| 技能-技能 | 新建技能 | 使用现有技能创建流程 |
| 技能-连接器 | 新建连接器 | 使用连接器创建流程 |
| 设置 | 新建会话 | 返回安全默认值 |

- [ ] 主导航切换后文字立即变化，不需要刷新或再次点击。
- [ ] 中间栏加号与左上角按钮调用同一个创建流程。
- [ ] 空状态里的创建按钮也调用同一个创建流程。

## 3. 会话页面

### 3.1 列表与选中态

- [ ] 会话列表作为所有资源列表的视觉基准。
- [ ] 单选、Cmd/Ctrl 多选、Shift 连选、右键选中均检查。
- [ ] 选中行有一致的背景、左侧指示、选择器、文字颜色和菜单显隐。
- [ ] 悬浮背景与 AI 推荐按钮、执行按钮等同级控件一致。
- [ ] 行间分隔线起点与图标/正文列对齐。

### 3.2 输入框与 AI 推荐

- [ ] AI 推荐按钮与左侧“执行/当前会话”等同级按钮使用相同边框、圆角和悬浮色。
- [ ] 推荐菜单不改变输入框高度，不遮挡附件和发送按钮。
- [ ] 键盘焦点、Enter 发送、Shift+Enter 换行、Esc 关闭菜单均正常。

### 3.3 会话菜单

- [ ] “Pin session”等英文全部汉化为“置顶会话/取消置顶”。
- [ ] 禁用项、危险项和普通项视觉符合统一菜单规范。
- [ ] 右键菜单与标题筛选菜单不会互相残留或重叠。

## 4. 浏览器页面

### 4.1 标签列表

- [ ] 中间栏标题为“所有标签”，不能显示“所有会话”。
- [ ] 选中标签复用会话选中语言：圆形选择器、选中背景、左侧强调条。
- [ ] 标签图标、标题、URL、副操作和关闭按钮列对齐。
- [ ] 关闭按钮只在悬浮/选中时按规范出现，不挤压标题。
- [ ] 置顶、静音、加载中和空白页状态均有明确但克制的反馈。

### 4.2 新标签页外观

- [ ] App 外观为自动/浅色/深色时，新标签页读取 App 最终解析后的外观。
- [ ] 切换外观无需重启即可更新新标签页。
- [ ] 新标签页输入框、文本、背景和焦点环与 App 主题一致。
- [ ] 不再直接跟随 macOS 系统主题覆盖 App 内选择。

### 4.3 原生层级

以下状态必须逐个实际打开验证：

- [ ] 标签右键菜单在网页上方。
- [ ] 标题筛选菜单在网页上方。
- [ ] 子菜单在网页上方。
- [ ] Dialog 在网页上方。
- [ ] Popover/Select 在网页上方。
- [ ] Toast/通知在网页上方。
- [ ] 右侧会话面板在网页上方且边界正确。
- [ ] 菜单关闭后原生网页恢复显示和交互。
- [ ] 切换到非浏览器模块后原生网页完全隐藏。

## 5. 项目页面

### 5.1 列表和详情

- [ ] 中间栏使用统一资源列表，不以大卡片代替列表。
- [ ] 选择项目后右侧显示完整项目详情，而非一行摘要。
- [ ] 项目文件、会话、自动化、上下文等低频信息使用详情分区或信息入口。

### 5.2 新建项目

- [ ] 左上角、标题加号、空状态入口均打开同一个自然语言创建面板。
- [ ] 用户只描述项目目标，不要求先填名称、Slug、目录或内部字段。
- [ ] 第一行可生成名称，完整描述保存为项目上下文。
- [ ] Enter 创建，Shift+Enter 换行，Esc/关闭按钮取消。
- [ ] 提交中按钮禁用并显示清晰状态；失败保留用户输入。

## 6. 自动化页面

### 6.1 页面结构

- [ ] 继续使用自动化列表 + 自动化详情。
- [ ] Workflow 编辑器、入口、筛选和空状态全部隐藏。
- [ ] 不显示无法理解或无法完成的“工作流”半成品功能。
- [ ] 修改自动化时保留可追踪的操作日志/执行记录入口。

### 6.2 新建自动化

- [ ] 左上角、标题加号、列表空状态使用同一个自然语言创建面板。
- [ ] 用户描述“什么时候、自动做什么”，系统推断触发条件和执行内容。
- [ ] 推断结果进入统一检查页，不跳到另一套风格的技术表单。
- [ ] 对模型推断出的 workflow 自动降级为普通定时/事件/Webhook 自动化。
- [ ] 保存前可检查触发条件、执行内容、重试和权限模式。
- [ ] 保存成功后列表立即出现并自动选中；失败时不清空描述。

## 7. 知识库页面

### 7.1 标题与操作

- [ ] 删除标题下重复标题、副标题和介绍文案。
- [ ] 删除正文中的重复“新建 Markdown 文档”按钮。
- [ ] 新建动作只保留左上角动态按钮和约定的空状态入口。
- [ ] 导入、思维导图、分类筛选、搜索全部进入标题右侧菜单。
- [ ] 菜单样式与会话/浏览器标题菜单完全一致。

### 7.2 文档列表和详情

- [ ] 文档列表学习会话列表：统一行高、图标、标题、摘要、时间和选中态。
- [ ] 文档正文区使用完整详情布局，不堆砌独立边框卡片。
- [ ] 来源段落、关联会话、元数据等低频内容进入右上角信息入口或详情分区。
- [ ] 编辑、更多、信息等按钮使用同一套小图标按钮。
- [ ] 长文、空文档、加载、保存中、保存失败、只读状态均核对。

## 8. 动态页面

- [ ] 页面保持三栏结构。
- [ ] 中间栏标题为“动态”，不得显示“所有会话”。
- [ ] 中间栏必须有动态列表或明确空状态，不能整列空白。
- [ ] 选择一条动态后右侧显示详情和关联对象。
- [ ] 全部、未读、已读、类型筛选进入标题右侧菜单。
- [ ] 标记已读后列表状态、计数和筛选结果立即同步。
- [ ] 点击关联会话/项目能正确跳转，返回后保持动态筛选状态。

## 9. 技能 / 专家 / 连接器

### 9.1 统一三栏交互

- [ ] 左侧主导航选择“技能”后，中间栏可在专家、技能、连接器间切换。
- [ ] 切换入口沿用原会话/浏览器标题菜单交互，不另做顶部大 Tab。
- [ ] 中间栏选择对象，右侧始终显示该对象详情。
- [ ] 切换类型后，左上角动态新建文字同步变化。
- [ ] 搜索、来源筛选、启用状态、发现能力等放入标题右侧菜单。

### 9.2 技能详情

- [ ] 必须使用原 `SkillInfoPage` 完整详情。
- [ ] 显示名称、描述、Slug、来源、位置、所需来源、权限模式和完整指令。
- [ ] 不能退化为只有名称、描述和“已安装”的摘要卡。
- [ ] 内置技能、项目技能、工作空间技能的可编辑/可删除状态正确。

### 9.3 专家创建与详情

- [ ] 新建专家只提供自然语言输入，不先展示系统提示词、模型、连接器 ID 等表单。
- [ ] 自动生成名称、说明和系统指令。
- [ ] 创建后可在完整详情中继续编辑模型、记忆、技能和连接器。
- [ ] 专家详情至少展示职责、系统指令、模型/连接、记忆规则、技能和连接器。

### 9.4 连接器详情

- [ ] 左侧选择连接器，右侧显示名称、类型、提供方、状态、连接信息和权限。
- [ ] 启用/停用、测试、编辑和删除操作集中在详情头部菜单。
- [ ] 密钥和敏感值不直接明文显示。

## 10. 设置页面

### 10.1 导航

- [ ] 进入设置默认选中“通用”，不能出现中间栏无选中状态。
- [ ] 只展示高频且用户可理解的设置。
- [ ] 当前保留：通用、模型、用户记忆、外观、输入、快捷键、空间、账号、权限。
- [ ] 标签、隐私、消息、服务端、认知层等低价值/技术页面不出现在普通导航。
- [ ] 隐藏导航不等于删除底层能力；深链访问时仍需有安全降级。

### 10.2 双 Command 截图

- [ ] 快捷键页有独立“屏幕截图”分组。
- [ ] 显示快捷键总状态：已就绪、未授权、不可用、已关闭、启动中。
- [ ] 分别显示“辅助功能权限”和“屏幕录制权限”。
- [ ] 未授权时出现“去授权”，并直达对应 macOS 系统设置。
- [ ] 授权后返回 App，可点击“重新检测”重启监听。
- [ ] 同时按左右 Command 后只把截图加入当前会话输入框，不自动发送。
- [ ] App 在前台和后台时分别验证。
- [ ] 无当前会话、无当前窗口、多窗口、权限撤销、截图失败均有明确反馈。

## 11. 统一交互状态核对

每个按钮、列表项、输入框和菜单至少检查：

- [ ] 默认态
- [ ] Hover
- [ ] Pressed
- [ ] Focus visible
- [ ] Selected
- [ ] Disabled
- [ ] Loading
- [ ] Empty
- [ ] Error
- [ ] Success/Toast
- [ ] 键盘操作
- [ ] 触控板/右键操作

统一规则：

- [ ] 图标按钮必须有 `aria-label` 和 Tooltip。
- [ ] 菜单项目支持键盘上下选择、Enter 执行、Esc 关闭。
- [ ] 危险操作使用统一 destructive 样式，并在必要时确认。
- [ ] 禁用项不能只降低透明度，还要不可点击、不可聚焦。
- [ ] Loading 期间防止重复提交。
- [ ] Toast 不被原生浏览器层遮挡。
- [ ] 对话框关闭后焦点返回触发按钮。

## 12. 主题、尺寸与可访问性矩阵

### 12.1 必测组合

| 维度 | 状态 |
|---|---|
| 外观 | 自动、浅色、深色 |
| 窗口 | 最小支持宽度、常规宽度、全屏 |
| 内容 | 空数据、少量数据、大量数据、超长标题 |
| 输入 | 鼠标、触控板、键盘 |
| 系统 | macOS 辅助功能已授权/未授权，屏幕录制已授权/未授权 |

### 12.2 可访问性

- [ ] 文字和背景对比度清楚，深浅主题均检查。
- [ ] 不能只靠颜色表达选中、错误、启用或未读。
- [ ] 键盘焦点始终可见。
- [ ] 重要图标有可读名称；装饰图标对辅助技术隐藏。
- [ ] 200% 缩放后不遮挡关键按钮和输入。
- [ ] 动态内容更新有可理解的状态反馈。
- [ ] 截图只能辅助判断；键盘顺序、读屏名称和实际权限行为必须单独测试。

## 13. 建议的完整跑测顺序

每一步都要保存“进入页面、打开菜单/弹窗、选择对象后详情”三类证据：

1. 会话：列表、筛选、右键、AI 推荐、输入框。
2. 浏览器：主题、标签选中、右键菜单、通知、右侧面板。
3. 项目：列表、详情、三个新建入口、创建失败。
4. 自动化：列表、详情、三个新建入口、推断检查、保存。
5. 知识库：标题菜单、列表、文档详情、信息入口。
6. 动态：列表、已读筛选、详情、关联跳转。
7. 技能-专家：切换、自然语言创建、完整详情。
8. 技能-技能：切换、完整原详情、权限与来源。
9. 技能-连接器：切换、状态、详情和操作。
10. 设置：默认通用、精简导航、主题切换。
11. 设置-快捷键：两项权限状态、跳转授权、重新检测、实际截图。
12. 全局：最窄窗口、全屏、浅色、深色、键盘完整走一遍。

## 14. 问题记录模板

每发现一个问题，按以下格式记录，避免只写“难看”：

```md
### [P1/P2/P3] 模块 - 问题名称

- 路由/页面：
- 当前状态：
- 触发步骤：
- 实际结果：
- 期望原生基准：
- 应复用组件：
- 功能影响：
- 主题/窗口范围：
- 截图：
- 修复后验证：
```

优先级定义：

- **P1**：主流程不可用、内容被遮挡、错误创建/删除、权限功能完全不可达。
- **P2**：布局错误、入口不一致、详情缺失、主题错误、键盘无法完成主流程。
- **P3**：间距、文字、图标、悬浮色、边框、次要状态不一致。

## 15. 单页通过标准

一个页面只有同时满足以下条件才算通过：

- [ ] 页面结构与原生基准一致。
- [ ] 所有入口调用同一套交互。
- [ ] 默认、选中、悬浮、菜单、空、加载、错误状态完整。
- [ ] 浅色/深色和至少两种窗口宽度通过。
- [ ] 鼠标和键盘主流程均可完成。
- [ ] 没有浏览器原生层或其他浮层遮挡。
- [ ] 没有重复标题、重复按钮、无意义说明或内部技术字段。
- [ ] 截图前后对比完成，问题修复后重新截图验证。

## 16. 前两轮已经明确修过、但必须回归的项目

- [ ] 左上角动态新建文字与行为。
- [ ] 会话 AI 推荐按钮样式。
- [ ] 会话置顶菜单汉化。
- [ ] 浏览器“所有标签”、新标签主题、标签选中态。
- [ ] 浏览器右键/标题菜单/通知层级。
- [ ] 自动化隐藏 Workflow、恢复列表和详情。
- [ ] 项目、专家、自动化自然语言创建。
- [ ] 知识库标题精简、操作菜单化、文档详情重排。
- [ ] 动态三栏、正确标题、列表和筛选菜单。
- [ ] 专家/技能/连接器三栏切换。
- [ ] 技能恢复完整 `SkillInfoPage`。
- [ ] 设置默认“通用”和导航精简。
- [ ] 双 Command 截图权限状态、授权入口与重新检测。

这些项目即使曾经通过，也必须在最新整合版本中重新验证；不能把上一次截图当作本次通过证据。
